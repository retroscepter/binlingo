{
  "name": "binlingo",
  "version": "1.2.0",
  "description": "Speak binary with JavaScript! A binary reader and writer utility.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "lint": "npx prettier --write src",
    "build": "npx tsc -p tsconfig.json",
    "build:watch": "npx tsc -w -p tsconfig.json",
    "build:docs": "npm run build && npm run docs",
    "build:test": "npm run build && npm run test",
    "build:bundle": "npm run build && npm run bundle",
    "bundle": "npx webpack",
    "docs": "npx jsdoc2md lib/constants.js lib/Reader.js lib/Writer.js --template README.hbs > README.md",
    "test": "npm run test:browser && npm run test:node",
    "test:browser": "jest --env=jsdom",
    "test:node": "jest --env=node",
    "prepublishOnly": "npm run build:bundle"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/octavetoast/binlingo.git"
  },
  "author": "Braden Lamb",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/octavetoast/binlingo/issues"
  },
  "homepage": "https://github.com/octavetoast/binlingo#readme",
  "devDependencies": {
    "@types/node": "^14.14.28",
    "jest": "^26.6.3",
    "jsdoc-to-markdown": "^6.0.1",
    "prettier": "^2.2.1",
    "typescript": "^4.1.5",
    "webpack": "^5.24.3",
    "webpack-cli": "^4.5.0"
  }
}
